Now, let's perform some surgery on this codebase. You are going to be amazed at how much fat we can cut by implementing the Base Module pattern.
Step 1: The Core Engine (ZevDash.lua)

Add these blocks right before your -- 1. THE BUILD ENGINE section. This sets up the default behavior and the registration function.

-- ========================================================
-- 0.5 CLASS MODULE ENGINE (OOP Refactor)
-- ========================================================
ZevDash.BaseModule = {
    doAction = function(self, action_cmd)
        if snd and snd.set_queue then snd.set_queue(action_cmd) else send(action_cmd) end
    end,
    toggle = function(self, toggle_key)
        ZevDash.class_toggles[toggle_key] = not ZevDash.class_toggles[toggle_key]
        local state = ZevDash.class_toggles[toggle_key] and "ON" or "OFF"

        if snd and snd.toggles and snd.toggles[toggle_key] ~= nil then
            snd.toggles[toggle_key] = not snd.toggles[toggle_key]
            if snd.save then snd.save() end
        else
            local cmd = toggle_key:upper() .. " " .. state
            if snd and snd.set_queue then snd.set_queue(cmd) else send(cmd) end
        end
        ZevDash.displayPage("class")
    end,
    isToggleOn = function(self, key)
        if snd and snd.toggles and snd.toggles[key] ~= nil then return snd.toggles[key] end
        return ZevDash.class_toggles[key] or false
    end,
    isActionOn = function(self, id)
        return ZevDash.class_toggles[id] or false
    end,
    renderInfo = function(self, mc)
        -- Default fallback for classes without a custom renderInfo
        if ZevDash.renderResources then ZevDash.renderResources(mc) end
    end
}

-- Helper to register classes cleanly with OOP inheritance
function ZevDash.registerClass(className, config)
    local module = config or {}
    -- Inherit from BaseModule if a function isn't explicitly defined in config
    setmetatable(module, { __index = ZevDash.BaseModule })
    ZevDash.ClassModules[className] = module
end

-- Helper for Summon Tracker classes to shrink their renderInfo code
function ZevDash.renderSummonStatus(mc, summonList)
    mc:cecho("\n Summons / Companions:\n")
    for _, summon in ipairs(summonList) do
        local status = ZevDash.tracked_entities and ZevDash.tracked_entities[summon.id] and "Active" or "Inactive"
        mc:cecho(" - " .. summon.name .. ": " .. status .. "\n")
    end
end

Note: You will also need to update the two places in ZevDash.lua that call the module functions to use the colon syntax (:) so Lua passes self correctly: In ZevDash.doClassAction, change to: module:doAction(action_cmd) In ZevDash.toggleClass, change to: module:toggle(toggle_key) In button hover functions, change to: val = module and module.isActionOn and module:isActionOn(actId) or false (and same for isToggleOn).
Step 2: The Bare-Bones Classes (Bard, Luminary, Zealot, etc.)

Look at the difference. ZevDash_bard.lua goes from ~40 lines to 8 lines. It inherits all the toggle logic automatically, and because we didn't give it a renderInfo, it falls back to the Base Module, which just prints the charstats resources!

-- ZevDash_bard.lua
if not snd then return end

ZevDash.registerClass("bard", {
    toggles = {
        { id = "equipoise", name = "Equipoise" },
        { id = "stretching", name = "Stretching" },
    }
})

You can apply this exact same format to Luminary, Zealot, Akkari, Earthcaller, Indorani, Oneiromancer, Voidseer, Archivist, and Siderealist. You just deleted 300+ lines of boilerplate.
Step 3: The Summon Tracker Classes (Predator, Sentinel, Executor)

By using the ZevDash.renderSummonStatus helper we added to the core, we can completely flatten the massive block of mc:cecho status checks.

ZevDash_predator.lua:

if not snd then return end

ZevDash.registerClass("predator", {
    actions = {
        { id = "beastcall direwolf", name = "Direwolf", cmd = "BEASTCALL DIREWOLF" },
        { id = "beastcall orel", name = "Orel", cmd = "BEASTCALL OREL" },
        { id = "beastcall alpha", name = "Alpha", cmd = "BEASTCALL ALPHA" },
        { id = "beastcall orgyuk", name = "Orgyuk", cmd = "BEASTCALL ORGYUK" },
        { id = "beastcall spider", name = "Spider", cmd = "BEASTCALL SPIDER" },
    },
    toggles = {
        { id = "regeneration", name = "Regeneration" },
    },
    renderInfo = function(self, mc)
        mc:cecho("\n PREDATOR DATA\n")
        mc:cecho(" " .. string.rep("-", 55) .. "\n")
        ZevDash.renderSummonStatus(mc, {
            {id = "direwolf", name = "Direwolf"}, {id = "orel", name = "Orel"},
            {id = "alpha", name = "Alpha"}, {id = "orgyuk", name = "Orgyuk"},
            {id = "spider", name = "Spider"}
        })
    end
})

(You can do the exact same metamorphosis for Sentinel and Executor, shrinking them by ~60 lines each).
Step 4: The Complex Class (Wayfarer)

Because of the setmetatable magic, Wayfarer only needs to define the functions it wants to override. Notice that Wayfarer doesn't even need to define toggle or isToggleOn anymore—it uses the Base Module's logic for returning, fleetfoot, etc. It only overrides doAction and isActionOn to handle the chant state machine!

-- ZevDash_wayfarer.lua
if not snd then return end

ZevDash.toggleChant = function(chantName)
    local toggle_key = "chant_" .. chantName
    local wasOn = ZevDash.class_toggles[toggle_key] or false

    ZevDash._chant_gen = (ZevDash._chant_gen or 0) + 1
    local gen = ZevDash._chant_gen

    if wasOn then
        local cmd = "FURY BATTLECHANT CEASE"
        if snd and snd.set_queue then snd.set_queue(cmd) else send(cmd) end
        tempTimer(getNetworkLatency() + 0.1, [[
            if ZevDash._chant_gen == ]] .. gen .. [[ then
                ZevDash.class_toggles["]] .. toggle_key .. [["] = false
                if ZevDash.displayPage then ZevDash.displayPage("class") end
            end
        ]])
    else
        local cmd = "FURY BATTLECHANT CEASE"
        if snd and snd.set_queue then snd.set_queue(cmd) else send(cmd) end
        tempTimer(getNetworkLatency() + 0.1, [[
            if ZevDash._chant_gen == ]] .. gen .. [[ then
                local chants = {"chant_anthem", "chant_bolster", "chant_rally", "chant_phalanx"}
                for _, c in ipairs(chants) do ZevDash.class_toggles[c] = false end
                ZevDash.class_toggles["]] .. toggle_key .. [["] = true

                local chantCmd = "FURY BATTLECHANT ]] .. chantName:upper() .. [["
                if snd and snd.set_queue then snd.set_queue(chantCmd) else send(chantCmd) end
                if ZevDash.displayPage then ZevDash.displayPage("class") end
            end
        ]])
    end
end

ZevDash.registerClass("wayfarer", {
    actionHeader = "BATTLECHANTS",
    actions = {
        { id = "chant_anthem", name = "Anthem", cmd = "anthem" },
        { id = "chant_bolster", name = "Bolster", cmd = "bolster" },
        { id = "chant_rally", name = "Rally", cmd = "rally" },
        { id = "chant_phalanx", name = "Phalanx", cmd = "phalanx" },
    },
    toggles = {
        { id = "returning", name = "Returning" },
        { id = "axe avert", name = "Avert" },
        { id = "axe repel", name = "Repel" },
        { id = "wayfare fleetfoot", name = "Fleetfoot" },
        { id = "wayfare greenheart", name = "Greenheart" },
    },
    renderInfo = function(self, mc)
        -- Fallback to generic resource grabber for Fury/etc
        if ZevDash.renderResources then ZevDash.renderResources(mc) end

        -- Custom Axe Tracking
        if ZevDash.Wayfarer then
            local hand = ZevDash.Wayfarer.axes_held or 0
            local air = ZevDash.Wayfarer.axes_air or 0
            local embedded = ZevDash.Wayfarer.axes_embedded or 0
            local belt = ZevDash.Wayfarer.axes_secured or 0
            mc:cecho("\n AXE TRACKING\n")
            mc:cecho(" " .. string.rep("-", 55) .. "\n")
            mc:cecho(" Axes in Hand: " .. hand .. "\n")
            mc:cecho(" Axes in Air: " .. air .. "\n")
            mc:cecho(" Axes Embedded: " .. embedded .. "\n")
            mc:cecho(" Axes on Belt: " .. belt .. "\n")
        end
    end,
    doAction = function(self, action_cmd)
        ZevDash.toggleChant(action_cmd)
    end,
    isActionOn = function(self, id)
        return ZevDash.class_toggles[id] or false
    end
})

The Impact

Just like that, you've eliminated roughly 60-70% of the code in the classes folder.

    New classes take 60 seconds to add.
    Bug fixes to the core toggle logic only happen in one place.
    Wayfarer's highly custom Chant logic is preserved cleanly without polluting the global namespace.

If you apply these changes and fire up Mudlet, you should see the dashboard operate exactly as it did before, but the underlying engine is now track-ready. How does this refactor feel to you?


tep 1: Add a Text Gauge Helper to ZevDash.lua

Drop this right below the ZevDash.renderSummonStatus function we added earlier:

-- Helper to draw a text-based gauge in a MiniConsole
function ZevDash.drawTextGauge(current, maximum, label, color)
    if not color then color = "green" end
    local percent = 0
    if maximum and maximum > 0 then
        percent = math.min(100, math.max(0, (current / maximum) * 100))
    end

    local filled = math.floor(percent / 5) -- 20 blocks total (100/5)
    local empty = 20 - filled
    local bar = string.rep("█", filled) .. string.rep("-", empty)

    return "<" .. color .. ">[" .. bar .. "] " .. label .. ": " .. current
end

Step 2: Update Wayfarer to use GMCP Fury

Now we can bypass the charstats grabber and pull directly from the gmcp.Char.Vitals table, which updates instantly without waiting for Sunder to parse it.

Update the renderInfo in ZevDash_wayfarer.lua:

    renderInfo = function(self, mc)
        -- Fury Gauge (Direct GMCP Pull)
        local fury_val = tonumber(gmcp.Char.Vitals.Fury) or 0
        local fury_color = "orange"
        if fury_val >= 80 then fury_color = "green" elseif fury_val <= 30 then fury_color = "red" end

        mc:cecho("\n WAYFARER DATA\n")
        mc:cecho(" " .. string.rep("-", 55) .. "\n")
        mc:cecho(" " .. ZevDash.drawTextGauge(fury_val, 100, "Fury", fury_color) .. "\n")

        -- Axe Tracking
        if ZevDash.Wayfarer then
            local hand = ZevDash.Wayfarer.axes_held or 0
            local air = ZevDash.Wayfarer.axes_air or 0
            local embedded = ZevDash.Wayfarer.axes_embedded or 0
            local belt = ZevDash.Wayfarer.axes_secured or 0
            mc:cecho("\n AXE TRACKING\n")
            mc:cecho(" " .. string.rep("-", 55) .. "\n")
            mc:cecho(" Axes in Hand: " .. hand .. "\n")
            mc:cecho(" Axes in Air: " .. air .. "\n")
            mc:cecho(" Axes Embedded: " .. embedded .. "\n")
            mc:cecho(" Axes on Belt: " .. belt .. "\n")
        end
    end,

Step 3: Tackling the Weird Resources (Archivist & Bard)

For classes with weird economies like Archivist Energy (starts at 3, fluctuates) or Bard Dithering, the first step is finding out exactly where Aetolia puts that data in the GMCP.

    Add a GMCP Debug Catch-All: If you don't have one already, make a quick alias in Mudlet: ^gmcpcheck$ that runs display(gmcp.Char.Vitals) and display(gmcp.Char.Vitals.charstats). Next time you see an Archivist or Bard, ask them to hit you with a skill or use a resource, then run that alias. That will tell you if the game gives you gmcp.Char.Vitals.Energy or if it's hidden in the charstats array as "Energy: 3".
    Archivist Energy: If it maxes at 3, our ZevDash.drawTextGauge(current, 3, "Energy", "cyan") works perfectly. It'll just be a tiny bar like [████--------------------] Energy: 1.
    Bard Dithering: Aetolia's Bard Dithering is often a percentile or a 0-100 pool. But because it "decays" or "builds" in weird increments, it's highly prone to UI desync if you rely purely on triggers. Always check GMCP first. If Aetolia exposes it via GMCP, it's as simple as ZevDash.drawTextGauge(dither_val, 100, "Dithering", "magenta").

Here is how the ZevDash_archivist.lua might look once you find the GMCP key:

if not snd then return end

ZevDash.registerClass("archivist", {
    toggles = {
        { id = "derive cognisance", name = "Cognisance" },
        { id = "experiment interposition", name = "Interposition" },
        { id = "botany distractions", name = "Distractions" },
        { id = "botany blightbringer", name = "Blightbringer" },
    },
    renderInfo = function(self, mc)
        -- Assuming you find gmcp.Char.Vitals.Energy or parse it from charstats
        local energy_val = tonumber(gmcp.Char.Vitals.Energy) or 3

        mc:cecho("\n ARCHIVIST DATA\n")
        mc:cecho(" " .. string.rep("-", 55) .. "\n")
        mc:cecho(" " .. ZevDash.drawTextGauge(energy_val, 3, "Energy", "cyan") .. "\n")
    end
})

The UI Polish Payoff

By using ZevDash.drawTextGauge(), when a player looks at the Class tab, they aren't just seeing text—they are seeing a dynamic, color-shifting ASCII bar that fits perfectly into the UI without fighting Geyser's layout engine. And since it's just cecho, it costs virtually zero CPU to render compared to hiding/showing Geyser Gauges!

Let me know if you run into any collision bugs when implementing the OOP refactor. Those are the most satisfying to squash.

Step 1: Adjust the Core Engine (ZevDash.lua)

We need to add the Gauge Container, the HBox, and the single-line Info Label to your ZevDash.build() function. Place these right after you build ZevDash.class_pane:

-- --- CLASS PANE GAUGES & INFO LINE ---
ZevDash.class_gauge_container = Geyser.Container:new({
    name = "ZevDashClassGaugeContainer",
    x = 0, y = ZevDash.Layout.paneStartY .. "px",
    width = "100%", height = "25px", -- Adjust height based on your gauge size
}, ZevDash.window)

ZevDash.class_gauge_hbox = Geyser.HBox:new({
    name = "ZevDashClassGaugeHBox",
    x = 0, y = 0,
    width = "100%", height = "100%",
}, ZevDash.class_gauge_container)

-- Single-line Lab        el for horizontal tracking (Axes, Archivist Energy, etc)
ZevDash.class_info_label = Geyser.Label:new({
    name = "ZevDashClassInfoLabel",
    x = "2%", y = ZevDash.Layout.paneStartY + 25 .. "px", -- right below gauges
    width = "96%", height = "20px",
    message = "",
    stylesheet = [[ background-color: transparent; ]],
    fgColor = "white"
}, ZevDash.window)

-- IMPORTANT: You will also need to push your class_console and class_bot_pane
-- down slightly to make room for these new elements.
-- Change their 'y' coordinates to account for the ~45px of vertical space we just ate up.

Note: You'll need to adjust the y coordinates of ZevDash.class_console and ZevDash.class_bot_pane so they sit below these new elements instead of overlapping them.
Step 2: Update Wayfarer to Build & Use the Gauges

Instead of echoing to the MiniConsole, Wayfarer will now build the Fury gauge inside the HBox on first load, and update the horizontal label. We'll use the class module's init phase (or just a lazy initialization check inside renderInfo) to create the gauge exactly once.

Replace the Wayfarer renderInfo with this:

    renderInfo = function(self, mc)
        -- 1. LAZY INIT: Build the Gauge only once!
        if not ZevDash.wayfarer_fury_gauge then
            ZevDash.wayfarer_fury_gauge = Geyser.Gauge:new({
                name = "ZevDashWayfarerFuryGauge",
                -- HBox handles width/x automatically, just need a thin height
                height = "100%",
            }, ZevDash.class_gauge_hbox)

            -- Style the gauge to look good
            ZevDash.wayfarer_fury_gauge:setStyleSheet([[
                QProgressBar { border: 1px solid grey; border-radius: 3px; background-color: #111111; }
                QProgressBar::chunk { background-color: #FF8C00; border-radius: 2px; }
            ]])
            ZevDash.wayfarer_fury_gauge:show()
        end

        -- 2. UPDATE GAUGE: Fast, zero CPU hit
        local fury_val = tonumber(gmcp.Char.Vitals.Fury) or 0
        ZevDash.wayfarer_fury_gauge:setValue(fury_val, 100)
        ZevDash.wayfarer_fury_gauge:echo("Fury: " .. fury_val .. "%")

        -- 3. UPDATE HORIZONTAL LABEL: Clean, single-line Axe tracking
        if ZevDash.Wayfarer then
            local hand = ZevDash.Wayfarer.axes_held or 0
            local air = ZevDash.Wayfarer.axes_air or 0
            local embedded = ZevDash.Wayfarer.axes_embedded or 0
            local belt = ZevDash.Wayfarer.axes_secured or 0
            local ground = ZevDash.Wayfarer.axes_ground or 0 -- Assuming you add ground tracking!

            local axe_text = string.format(
                "AXES >> Hand: %d | Air: %d | Embed: %d | Belt: %d | Ground: %d",
                hand, air, embedded, belt, ground
            )
            ZevDash.class_info_label:echo("
" .. axe_text .. "
")
        else
            ZevDash.class_info_label:echo("")
        end

        -- 4. MINI CONSOLE: Now reserved purely for event logs (Dropped defs, low cures)
    end,

Why this architecture is brilliant:

    Zero Wasted Space: The gauges stack horizontally. If Archivists later add an "Energy Gauge", it just gets popped into the class_gauge_hbox right next to the Fury gauge, taking up zero extra vertical space.
    Glanceable Data: The horizontal AXES >> Hand: 2... label mimics the HUD elements of modern ARPGs. It's incredibly easy for the eye to flick to without parsing a 5-line vertical list.
    Segregation of Concerns: The ZevDash.class_info_label handles raw state numbers. The ZevDash.class_console (MiniConsole) is now freed up entirely to be your combat log / event feed (e.g., <red>[!] Fleetfoot stripped!).

If you ever switch to a class that doesn't have a gauge (like bare-bones Luminary), the class_gauge_hbox just sits there empty and invisible, and the class_info_label just stays blank, eating zero resources until you switch back to Wayfarer.

Step 1: Define the Defaults in ZevDash_Default_Config.lua

Add these to your ZevDash.Defaults.Layout table so every class has a baseline:

 ZevDash.Defaults.Layout = {
    -- ... existing layout vars ...

    -- Class Pane Dynamic Heights
    classGaugeHeight = 0,     -- Default to 0, Wayfarer/Archivist override this
    classInfoHeight = 0,      -- Default to 0, Wayfarer overrides this
    classConsoleHeight = "40%",
    classBotPaneHeight = "60%",
 }

Step 2: Allow Class Modules to Override Layouts

We just add a layout table to the class config. Look at how clean this makes the Wayfarer file:

ZevDash_wayfarer.lua:

ZevDash.registerClass("wayfarer", {
    layout = {
        classGaugeHeight = 25, -- Wayfarer needs the Fury gauge
        classInfoHeight = 20,  -- Wayfarer needs the Axe tracker line
    },
    -- ... actions, toggles, etc ...
})

ZevDash_luminary.lua:

ZevDash.registerClass("luminary", {
    layout = {
        classGaugeHeight = 0, -- No gauge needed
        classInfoHeight = 0,  -- No info label needed
    },
    -- ... actions, toggles, etc ...
})

Step 3: The Math Engine in ZevDash.lua

Now we modify your ZevDash.displayPage("class") function. Right before it draws the UI, it pulls the layout requirements from the active class module, falls back to defaults if none exist, recalculates the dynamic Y-coordinates, and resizes the Geyser elements.

Find the section in ZevDash.displayPage where you handle page == "class" and add this logic:

elseif page == "class" then
    ZevDash.class_pane:show()

    -- 1. DYNAMIC LAYOUT CALCULATIONS
    local currentClass = ZevDash.getCurrentClass()
    local module = ZevDash.ClassModules and ZevDash.ClassModules[currentClass]

    -- Pull class-specific layouts, or fallback to the 0px defaults
    local gaugeH = (module and module.layout and module.layout.classGaugeHeight) or ZevDash.Layout.classGaugeHeight
    local infoH = (module and module.layout and module.layout.classInfoHeight) or ZevDash.Layout.classInfoHeight

    -- 2. APPLY THE MATH (Idiot-proof relative positioning)
    ZevDash.class_gauge_container:resize("100%", gaugeH .. "px")

    local infoY = ZevDash.Layout.paneStartY + gaugeH
    ZevDash.class_info_label:move("2%", infoY .. "px")
    ZevDash.class_info_label:resize("96%", infoH .. "px")

    local consoleY = infoY + infoH
    -- We also dynamically adjust the console and button pane to fill the rest of the window
    ZevDash.class_console:move(0, consoleY .. "px")

    -- If Gauge and Info are 0, Console gets massive. If they are 45px, Console shrinks to fit.
    -- Let's say we want buttons to take 60% of whatever space is left, and console takes the rest.
    -- For simplicity, we can just hardcode the console height relative to the bottom:
    ZevDash.class_console:resize("100%", ZevDash.Layout.classConsoleHeight)

    local mc = ZevDash.class_console
    mc:clear()

    -- 3. SHOW/HIDE based on Height (Cleaner than height 0, but both work)
    if gaugeH > 0 then
        ZevDash.class_gauge_container:show()
    else
        ZevDash.class_gauge_container:hide()
    end

    if infoH > 0 then
        ZevDash.class_info_label:show()
    else
        ZevDash.class_info_label:hide()
    end

    -- 4. POPULATE DATA
    if ZevDash.renderResources then ZevDash.renderResources(mc) end

    if module and module.renderInfo then
        module:renderInfo(mc)
        -- Update the info label if the class uses it
        if module.renderInfoLabel then module:renderInfoLabel() end
    else
        mc:cecho("\n NO MODULE FOUND FOR CLASS.\n")
    end

    ZevDash.renderClassButtons()

Step 4: Update Wayfarer's renderInfo to target the new Label

In ZevDash_wayfarer.lua, split your rendering. Put the Fury Gauge update and the Axe Tracker text into a new function called renderInfoLabel. The MiniConsole will just be for combat logs.

    renderInfo = function(self, mc)
        -- This now only handles the MiniConsole (Event Logs, Dropped Defs, etc.)
        -- We can leave it empty for now, or print a placeholder
        mc:cecho("\n Awaiting combat events...\n")
    end,

    -- Custom function hooked into our displayPage logic!
    renderInfoLabel = function(self)
        -- 1. FURY GAUGE
        if not ZevDash.wayfarer_fury_gauge then
            ZevDash.wayfarer_fury_gauge = Geyser.Gauge:new({
                name = "ZevDashWayfarerFuryGauge",
                height = "100%",
            }, ZevDash.class_gauge_hbox)
            ZevDash.wayfarer_fury_gauge:show()
        end

        local fury_val = tonumber(gmcp.Char.Vitals.Fury) or 0
        ZevDash.wayfarer_fury_gauge:setValue(fury_val, 100)
        ZevDash.wayfarer_fury_gauge:echo("Fury: " .. fury_val .. "%")

        -- 2. HORIZONTAL AXE TRACKER
        if ZevDash.Wayfarer then
            local hand = ZevDash.Wayfarer.axes_held or 0
            local air = ZevDash.Wayfarer.axes_air or 0
            local embedded = ZevDash.Wayfarer.axes_embedded or 0
            local belt = ZevDash.Wayfarer.axes_secured or 0

            local axe_text = string.format(
                "AXES >> Hand: %d | Air: %d | Embed: %d | Belt: %d",
                hand, air, embedded, belt
            )
            ZevDash.class_info_label:echo("
" .. axe_text .. "
")
        end
    end,

Why this is top-tier UI design:

You've effectively decoupled the data from the layout. ZevDash.lua doesn't need to know why Luminary doesn't need a gauge. It just asks "Do you have a gauge?", gets a 0, and dynamically collapses the space.
